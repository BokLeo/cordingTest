{
  "compilerOptions": {
    "target": "es6", // 컴파일된 JS의 ECMAScript 버전
		"lib": ["es2017", "dom", "es2019"],
    "module": "commonjs", // 모듈 시스템
    "outDir": "./", // 컴파일된 JS 파일이 저장될 폴더
    "strict": true, // 엄격한 타입 검사를 활성화
		"sourceMap" : true
  },
  "include": ["src/**/*"], // 컴파일할 파일의 경로
  "exclude": ["node_modules"], // 제외할 파일
	
	
}
